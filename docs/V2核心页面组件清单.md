# Archived V2 Component Inventory

This file is retained only as a historical design artifact.

It is not the current component contract for the app.

Why it is obsolete:

1. It describes a planned V2 decomposition, not the code that exists now.
2. It assumes component directories and page boundaries that do not match the current repository.
3. It predates the current route split across:
   - `/opportunity/*`
   - `/agent`
   - `/selection/*`

Use these documents for the current implementation:

- `docs/当前系统架构与技术实现说明.md`
- `docs/当前项目核心功能梳理.md`
- `docs/opportunity_workspace_deep_dive.md`
