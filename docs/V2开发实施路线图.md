# Archived V2 Implementation Roadmap

This file is kept only as historical planning context.

It is no longer an accurate description of the current repository state.

Reasons it is obsolete:

1. The canonical route model has changed.
   - opportunity pages now live under `/opportunity/*`
   - `/workspace` is only a legacy redirect
2. The repository now includes:
   - `/agent`
   - `/selection/*`
   - shared agent platform code
3. Several proposed component and service boundaries in the old V2 plan were never implemented in the exact form described there.

Use these documents instead for current truth:

- `README.md`
- `docs/当前系统架构与技术实现说明.md`
- `docs/当前项目核心功能梳理.md`
- `docs/opportunity_workspace_deep_dive.md`
